<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Luban Fragrance | The Essence of Elegance</title>
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Inter:wght@200;300;400;500&display=swap" rel="stylesheet">
    <!-- Lucide Icons -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --bg-dark: #020a09;
            --bg-accent: #051614;
            --gold-primary: #d4af37;
            --gold-bright: #f4d03f;
            --gold-muted: rgba(212, 175, 55, 0.6);
            --text-white: #ffffff;
            --text-dim: rgba(255, 255, 255, 0.6);
            --border-gold: rgba(212, 175, 55, 0.15);
            --transition-smooth: all 0.55s cubic-bezier(0.19, 1, 0.22, 1);
            --card-gradient: linear-gradient(145deg, rgba(255,255,255,0.03) 0%, rgba(255,255,255,0) 100%);
            --nav-height: 100px;
            --container-padding-desktop: 8%;
            --container-padding-mobile: 5%;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; -webkit-tap-highlight-color: transparent; -webkit-font-smoothing: antialiased; }

        /* Desktop-only custom cursor */
        @media (min-width: 1200px) {
            * { cursor: none; }
            #custom-cursor {
                width: 20px;
                height: 20px;
                border: 1px solid var(--gold-primary);
                border-radius: 50%;
                position: fixed;
                pointer-events: none;
                z-index: 9999;
                transition: transform 0.08s linear, background 0.2s;
                mix-blend-mode: difference;
                display: block;
            }
        }

        @media (max-width: 1199px) {
            #custom-cursor { display: none; }
            * { cursor: auto !important; }
        }

        body {
            background-color: var(--bg-dark);
            color: var(--text-white);
            font-family: 'Inter', sans-serif;
            overflow-x: hidden;
            line-height: 1.6;
            -webkit-font-smoothing: antialiased;
        }

        /* Typography */
        h1, h2, h3, .playfair { font-family: 'Playfair Display', serif; font-weight: 400; margin: 0; }
        p { margin: 0; }

        /* Scroll Progress */
        #scroll-progress {
            position: fixed;
            top: 0; left: 0;
            height: 3px;
            background: var(--gold-primary);
            z-index: 2001;
            width: 0%;
            transition: width 0.2s linear;
        }

        /* Navbar */
        nav {
            position: fixed;
            top: 0; width: 100%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 var(--container-padding-desktop);
            height: var(--nav-height);
            z-index: 1000;
            transition: var(--transition-smooth);
            background: transparent;
        }

        nav.scrolled {
            height: 75px; 
            background: rgba(2, 10, 9, 0.95);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border-gold);
        }

        .nav-logo-container { display: flex; align-items: center; height: 100%; width: 180px; min-width: 120px; }
        .nav-logo-img { height: 48px; width: auto; object-fit: contain; filter: drop-shadow(0 0 10px rgba(212, 175, 55, 0.08)); transition: height 0.35s; }
        nav.scrolled .nav-logo-img { height: 36px; }

        .nav-links { display: flex; gap: 2.2rem; list-style: none; flex: 1; justify-content: center; align-items: center; }
        .nav-links a { text-decoration: none; color: var(--text-dim); font-size: 0.78rem; text-transform: uppercase; letter-spacing: 2px; transition: color 0.2s, letter-spacing 0.2s; }
        .nav-links a:hover { color: var(--gold-primary); letter-spacing: 3px; }

        .nav-actions { display:flex; align-items:center; gap:1rem; min-width:160px; justify-content:flex-end; }
        .social-icons-nav { display:flex; gap:1rem; align-items:center; border-left:1px solid var(--border-gold); padding-left:1rem; }
        .social-link { color: var(--gold-primary); transition: var(--transition-smooth); opacity: 0.85; }
        .social-link:hover { opacity: 1; transform: translateY(-3px); }

        .menu-toggle { display: none; background: none; border: none; color: var(--gold-primary); font-size: 1.6rem; padding: 10px; border-radius: 8px; }

        /* Hero Section */
        #hero {
            height: clamp(62vh, 80vh, 100vh);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
            overflow: hidden;
            text-align: center;
            padding: 0 var(--container-padding-desktop);
        }

        .hero-bg-text {
            position: absolute;
            font-size: 18vw;
            font-family: 'Playfair Display';
            color: rgba(212, 175, 55, 0.03);
            white-space: nowrap;
            pointer-events: none;
            z-index: 0;
            user-select: none;
        }

        .hero-content { position: relative; z-index: 1; width: 100%; max-width: 920px; padding: 2rem 0; }
        #hero h1 { font-size: clamp(3rem, 11vw, 7rem); letter-spacing: 0.2em; margin-bottom: 0.5rem; background: linear-gradient(to bottom, #fff 40%, var(--gold-primary) 100%); -webkit-background-clip: text; -webkit-text-fill-color: transparent; opacity: 0; transform: translateY(30px); animation: revealUp 1s forwards 0.4s; }

        @keyframes revealUp { to { opacity: 1; transform: translateY(0); } }

        /* Buttons */
        .btn-gold {
            background: none; 
            border: 1px solid var(--gold-primary); 
            color: var(--gold-primary); 
            padding: 14px 28px; 
            text-transform: uppercase; 
            letter-spacing: 2.5px; 
            font-size: 0.8rem; 
            cursor: pointer; 
            transition: background 0.25s, color 0.25s, box-shadow 0.25s;
            position: relative;
            overflow: hidden;
            border-radius: 8px;
        }
        .btn-gold:hover { background: var(--gold-primary); color: #000; box-shadow: 0 6px 30px rgba(212, 175, 55, 0.18); }

        /* Size Selection */
        .size-selector { display:flex; gap:12px; margin-bottom: 26px; }
        .size-btn { flex: 1; padding: 10px; border: 1px solid var(--border-gold); background: transparent; color: var(--text-dim); cursor: pointer; transition: 0.22s; text-transform: uppercase; font-size: 0.85rem; letter-spacing: 1px; border-radius: 8px; }
        .size-btn.active { border-color: var(--gold-primary); color: var(--gold-primary); background: rgba(212, 175, 55, 0.04); }

        /* Product Grid with refined breakpoints */
        #shop { padding: 100px var(--container-padding-desktop); background: var(--bg-accent); transition: padding 0.3s; }
        .product-grid { display: grid; gap: 28px; grid-auto-rows: 1fr; }

        /* Default / small desktop (>=1200) */
        @media (min-width: 1400px) {
            .product-grid { grid-template-columns: repeat(4, 1fr); gap: 32px; }
        }

        /* Large tablets / small laptops */
        @media (min-width: 1024px) and (max-width: 1399px) {
            .product-grid { grid-template-columns: repeat(3, 1fr); gap: 28px; }
        }

        /* Tablets */
        @media (min-width: 768px) and (max-width: 1023px) {
            .product-grid { grid-template-columns: repeat(2, 1fr); gap: 22px; }
            nav { padding: 0 4.5%; }
            #shop { padding: 80px 4.5%; }
        }

        /* Mobile */
        @media (max-width: 767px) {
            .product-grid { grid-template-columns: 1fr; gap: 18px; }
            nav { padding: 0 var(--container-padding-mobile); height: 80px; }
            .nav-links { display: none; }
            .menu-toggle { display: block; }
            .nav-logo-img { height: 40px; }
            #hero { padding: 0 var(--container-padding-mobile); height: auto; min-height: 56vh; }
            .hero-content { padding-top: 1.2rem; padding-bottom: 1.2rem; }
            .hero-bg-text { display: none; }
            #shop { padding: 60px var(--container-padding-mobile); }
            .product-card { padding: 20px; border-radius: 12px; }
            .image-container { aspect-ratio: 1.05/1; }
            .menu-toggle { padding: 12px; }
            .btn-gold { width: 100%; padding: 14px; font-size: 0.9rem; }
        }

        .product-card {
            position: relative;
            padding: 24px;
            background: var(--card-gradient);
            border: 1px solid var(--border-gold);
            transition: var(--transition-smooth);
            overflow: hidden;
            display: flex;
            flex-direction: column;
            cursor: pointer;
            border-radius: 12px;
        }
        .product-card:hover { transform: translateY(-8px); border-color: var(--gold-primary); box-shadow: 0 10px 30px rgba(0,0,0,0.3); }

        .image-container {
            width: 100%;
            aspect-ratio: 1/1;
            overflow: hidden;
            margin-bottom: 18px;
            position: relative;
            background: radial-gradient(circle, rgba(255,255,255,0.04) 0%, rgba(0,0,0,0.18) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 10px;
        }

        .image-container img {
            max-width: 85%;
            max-height: 85%;
            object-fit: contain;
            transition: transform 0.9s cubic-bezier(0.19, 1, 0.22, 1);
        }
        .product-card:hover .image-container img { transform: scale(1.06); }

        /* Contact Section */
        #contact { padding: 100px var(--container-padding-desktop); background: var(--bg-dark); transition: padding 0.3s; }
        .contact-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: start; }
        @media (max-width: 1024px) { .contact-grid { grid-template-columns: 1fr; } }

        /* Checkout Layout */
        #checkout-view { padding: 80px var(--container-padding-desktop); min-height: 80vh; }
        .checkout-grid { display: grid; grid-template-columns: 1fr 360px; gap: 36px; align-items: start; }
        @media (max-width: 980px) { .checkout-grid { grid-template-columns: 1fr; } }

        .checkout-panel { background: var(--bg-accent); border: 1px solid var(--border-gold); padding: 28px; border-radius: 12px; }
        .form-row { display:flex; gap:12px; }
        .form-row .col { flex:1; }
        .input, textarea, select { width:100%; background:transparent; color:var(--text-white); border:1px solid rgba(255,255,255,0.03); padding:12px; border-radius:8px; outline:none; }
        .input::placeholder, textarea::placeholder { color: rgba(255,255,255,0.28); }

        .order-summary { display:flex; flex-direction:column; gap:12px; }
        .summary-row { display:flex; justify-content:space-between; color:var(--text-dim); }
        .summary-total { display:flex; justify-content:space-between; font-weight:700; color:var(--gold-primary); font-size:1.25rem; margin-top:10px; }

        /* Mobile Menu Overlay */
        #mobile-menu {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100vh;
            background: var(--bg-dark);
            z-index: 1001;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 1.8rem;
            transform: translateY(-100%);
            transition: transform 0.5s cubic-bezier(0.19, 1, 0.22, 1);
            padding: 2.5rem var(--container-padding-mobile);
        }
        #mobile-menu.active { transform: translateY(0); }
        .mobile-link { font-family: 'Playfair Display'; font-size: 1.7rem; color: var(--gold-primary); text-decoration: none; letter-spacing: 5px; }

        /* Floating Controls */
        #back-to-top, #whatsapp-float {
            position: fixed;
            right: 20px;
            width: 52px;
            height: 52px;
            border: 1px solid var(--gold-primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--gold-primary);
            z-index: 100;
            background: rgba(2, 10, 9, 0.9);
            backdrop-filter: blur(5px);
            transition: transform 0.18s, opacity 0.18s;
        }
        #back-to-top { bottom: 20px; opacity: 0; pointer-events: none; }
        #back-to-top.visible { opacity: 1; pointer-events: all; transform: translateY(-6px); }
        #whatsapp-float { bottom: 85px; border-color: #25D366; color: #25D366; text-decoration: none; }

        /* Details View Custom */
        .details-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; align-items: center; }
        @media (max-width: 900px) { .details-grid { grid-template-columns: 1fr; gap: 28px; } #details-view { padding-top: 100px !important; } }

        /* Cart Overlay & Drawer — responsive behavior */
        #cart-overlay {
            position: fixed;
            top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.9);
            backdrop-filter: blur(10px);
            z-index: 2000;
            visibility: hidden;
            opacity: 0;
            transition: opacity 0.35s ease, visibility 0.35s;
            display: block;
        }

        .cart-drawer {
            position: fixed;
            top: 0;
            right: -100%;
            width: 100%;
            max-width: 420px;
            height: 100%;
            background: #020a09;
            padding: 36px;
            transition: right 0.45s cubic-bezier(0.19, 1, 0.22, 1);
            box-shadow: -20px 0 60px rgba(0,0,0,0.6);
            overflow: auto;
        }

        /* Active state using class for predictable positioning */
        #cart-overlay.open { visibility: visible; opacity: 1; }
        #cart-overlay.open .cart-drawer { right: 0; }

        /* Mobile: drawer slides from bottom */
        @media (max-width: 767px) {
            .cart-drawer {
                top: auto;
                right: 0;
                left: 0;
                bottom: -100%;
                max-width: 100%;
                height: auto;
                max-height: 72vh;
                border-top-left-radius: 14px;
                border-top-right-radius: 14px;
                padding: 22px;
                transition: bottom 0.35s cubic-bezier(0.19, 1, 0.22, 1);
            }
            #cart-overlay.open .cart-drawer { bottom: 0; }
        }

        footer { padding: 60px var(--container-padding-desktop); text-align:center; border-top:1px solid var(--border-gold); transition: padding 0.3s; }
        @media (max-width: 767px) { footer { padding: 40px var(--container-padding-mobile); } }

        /* Small UI polish */
        input, textarea, button { font-family: inherit; }
        a { color: inherit; }
    </style>
</head>
<body>

    <div id="custom-cursor" aria-hidden="true"></div>
    <div id="scroll-progress" role="progressbar" aria-hidden="true"></div>
    
    <!-- Mobile Navigation -->
    <div id="mobile-menu" aria-hidden="true">
        <button onclick="toggleMobileMenu()" aria-label="Close menu" style="position:absolute; top:18px; right:18px; background:none; border:none; color:var(--gold-primary);"><i data-lucide="x" size="28"></i></button>
        <a href="javascript:void(0)" class="mobile-link" onclick="handleNavClick('hero'); toggleMobileMenu();">The Scent</a>
        <a href="javascript:void(0)" class="mobile-link" onclick="handleNavClick('shop'); toggleMobileMenu();">Collection</a>
        <a href="javascript:void(0)" class="mobile-link" onclick="handleNavClick('about'); toggleMobileMenu();">Heritage</a>
        <a href="javascript:void(0)" class="mobile-link" onclick="handleNavClick('contact'); toggleMobileMenu();">Contact</a>
        <div style="display:flex; gap: 20px; margin-top: 22px;">
            <a href="https://www.facebook.com/people/Luban-Fragrance/61586484638332/" target="_blank" style="color:var(--gold-primary)"><i data-lucide="facebook"></i></a>
            <a href="https://www.instagram.com/lubanfragrance" target="_blank" style="color:var(--gold-primary)"><i data-lucide="instagram"></i></a>
        </div>
    </div>

    <div id="back-to-top" onclick="window.scrollTo({top: 0, behavior: 'smooth'})" role="button" aria-label="Back to top"><i data-lucide="chevron-up"></i></div>
    <a id="whatsapp-float" href="https://wa.me/8801935322213?text=Hello%2C%20I'm%20interested%20in%20Luban%20Fragrance!" target="_blank" aria-label="Whatsapp contact">
        <i data-lucide="phone"></i>
    </a>

    <nav id="navbar" role="navigation" aria-label="Main navigation">
        <div onclick="showView('main')" class="nav-logo-container" style="cursor:pointer" role="button" aria-label="Go to homepage">
            <img src="https://i.ibb.co.com/Vcq9NZsL/Logo.png" alt="Luban Logo" class="nav-logo-img">
        </div>
        
        <ul class="nav-links" role="menubar">
            <li role="none"><a role="menuitem" href="javascript:void(0)" onclick="handleNavClick('hero')">The Scent</a></li>
            <li role="none"><a role="menuitem" href="javascript:void(0)" onclick="handleNavClick('shop')">Collection</a></li>
            <li role="none"><a role="menuitem" href="javascript:void(0)" onclick="handleNavClick('about')">Heritage</a></li>
            <li role="none"><a role="menuitem" href="javascript:void(0)" onclick="handleNavClick('contact')">Contact</a></li>
        </ul>

        <div class="nav-actions">
            <div class="social-icons-nav" aria-hidden="false">
                <a href="https://www.facebook.com/people/Luban-Fragrance/61586484638332/" target="_blank" class="social-link" aria-label="Facebook"><i data-lucide="facebook" size="18"></i></a>
                <a href="https://www.instagram.com/lubanfragrance" target="_blank" class="social-link" aria-label="Instagram"><i data-lucide="instagram" size="18"></i></a>
            </div>
            <div class="cart-trigger" onclick="toggleCart(true)" style="cursor:pointer; position:relative; margin-left: 10px;" role="button" aria-label="Open cart">
                <i data-lucide="shopping-cart" stroke="#d4af37" size="22"></i>
                <span id="cart-count" style="position:absolute; top:-8px; right:-10px; background:var(--gold-primary); color:#000; font-size:10px; padding:2px 6px; border-radius:10px; font-weight:800;">0</span>
            </div>
            <button class="menu-toggle" onclick="toggleMobileMenu()" aria-expanded="false" aria-controls="mobile-menu"><i data-lucide="menu"></i></button>
        </div>
    </nav>

    <div id="main-view">
        <section id="hero">
            <div class="hero-bg-text" aria-hidden="true">LUBAN</div>
            <div class="hero-content">
                <img src="https://i.ibb.co.com/Vcq9NZsL/Logo.png" alt="Luban" style="width: clamp(70px, 14vw, 120px); margin-bottom: 1.6rem; opacity: 0.85;">
                <h1>LUBAN</h1>
                <p style="letter-spacing: clamp(4px, 1vw, 10px); color: var(--gold-muted); text-transform: uppercase; font-size: clamp(0.6rem, 2vw, 0.8rem); margin-bottom: 2rem;">Refined Quality In Every Drop</p>
                <div style="display: flex; gap: 1rem; justify-content: center;">
                    <button class="btn-gold" onclick="document.getElementById('shop').scrollIntoView({behavior: 'smooth'})">Explore Collection</button>
                </div>
            </div>
        </section>

        <section id="shop">
            <div style="text-align: center; margin-bottom: 44px;">
                <h2 style="font-size: clamp(1.8rem, 4.4vw, 2.8rem); margin-bottom: 12px; letter-spacing: 5px;">The Library</h2>
                <div style="width: 40px; height: 1px; background: var(--gold-primary); margin: 0 auto;"></div>
            </div>
            <div class="product-grid" id="product-list" aria-live="polite"></div>
        </section>

        <section id="about" style="padding: 100px var(--container-padding-desktop); border-top: 1px solid var(--border-gold);">
            <div class="about-content" style="display: grid; grid-template-columns: 1fr 1fr; gap: 48px; align-items: center;">
                <div style="position: relative;">
                    <div style="border: 1px solid var(--border-gold); padding: 48px; display: flex; justify-content: center; background: var(--bg-accent); border-radius: 12px;">
                        <img src="https://i.ibb.co.com/Vcq9NZsL/Logo.png" style="width: 60%; opacity: 0.15;">
                    </div>
                </div>
                <div>
                    <h2 style="font-size: clamp(2.2rem, 5vw, 3.2rem); color: var(--gold-primary); margin-bottom: 18px;">Our Heritage</h2>
                    <p style="color: var(--text-dim); line-height: 1.8; font-size: 1.02rem; margin-bottom: 14px;">Born from a passion for the ancestral art of scent, Luban Fragrance represents the pinnacle of olfactory craftsmanship. We believe that a fragrance is a silent biography.</p>
                    <p style="color: var(--text-dim); line-height: 1.8; font-size: 1.02rem;">By bypassing the excessive marketing of traditional luxury houses, we invest solely in what matters: the pure essence of the juice.</p>
                </div>
            </div>
        </section>

        <section id="contact">
            <div class="contact-grid">
                <div>
                    <h2 style="font-size: clamp(2rem, 4.6vw, 3rem); color: var(--gold-primary); margin-bottom: 20px;">Get in Touch</h2>
                    <p style="color: var(--text-dim); margin-bottom: 28px; line-height: 1.8;">Have a question about our collections or need assistance choosing your signature scent? Our experts are here to help.</p>
                    
                    <div style="display:flex; flex-direction:column; gap:20px;">
                        <div style="display:flex; align-items:center; gap:16px;">
                            <div style="width:50px; height:50px; border:1px solid var(--border-gold); display:flex; align-items:center; justify-content:center; color:var(--gold-primary); border-radius:8px;"><i data-lucide="map-pin"></i></div>
                            <div><p style="text-transform:uppercase; font-size:0.7rem; color:var(--gold-muted); letter-spacing:2px;">Location</p><p>Dhaka, Bangladesh</p></div>
                        </div>
                        <div style="display:flex; align-items:center; gap:16px;">
                            <div style="width:50px; height:50px; border:1px solid var(--border-gold); display:flex; align-items:center; justify-content:center; color:var(--gold-primary); border-radius:8px;"><i data-lucide="mail"></i></div>
                            <div><p style="text-transform:uppercase; font-size:0.7rem; color:var(--gold-muted); letter-spacing:2px;">Email</p><p>contact@lubanfragrance.com</p></div>
                        </div>
                    </div>
                </div>
                
                <div style="background: var(--bg-accent); padding: 40px; border: 1px solid var(--border-gold); border-radius: 12px;">
                    <h3 class="playfair" style="font-size: 1.4rem; margin-bottom: 18px;">Send a Message</h3>
                    <form onsubmit="event.preventDefault();" style="display: flex; flex-direction: column; gap: 14px;">
                        <input type="text" placeholder="Your Name" style="background: transparent; border: none; border-bottom: 1px solid var(--border-gold); padding: 12px 0; color: #fff; outline: none;">
                        <input type="email" placeholder="Your Email" style="background: transparent; border: none; border-bottom: 1px solid var(--border-gold); padding: 12px 0; color: #fff; outline: none;">
                        <textarea placeholder="Your Message..." rows="3" style="background: transparent; border: none; border-bottom: 1px solid var(--border-gold); padding: 12px 0; color: #fff; outline: none; resize: none;"></textarea>
                        <button type="submit" class="btn-gold" style="width:100%; margin-top:6px;">Send Inquiry</button>
                    </form>
                </div>
            </div>
        </section>
    </div>

    <div id="details-view" style="display: none; min-height: 100vh; padding: 140px var(--container-padding-desktop) 80px;">
        <div id="details-content"></div>
    </div>

    <!-- New Checkout View -->
    <section id="checkout-view" style="display:none;">
        <div style="max-width:1200px; margin:0 auto;">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:22px;">
                <a href="javascript:void(0)" onclick="showView('main')" style="color:var(--gold-primary); font-size:0.9rem; display:inline-flex; align-items:center; gap:8px;"><i data-lucide="arrow-left" size="14"></i> Back to Shopping</a>
                <h2 style="letter-spacing:2px;">Billing Details</h2>
            </div>

            <div class="checkout-grid">
                <div class="checkout-panel">
                    <form id="checkout-form" onsubmit="event.preventDefault(); placeOrder();">
                        <div style="margin-bottom:12px;">
                            <label style="font-size:0.72rem; color:var(--gold-muted); text-transform:uppercase; letter-spacing:1.6px;">Full Name *</label>
                            <input id="billing-name" class="input" type="text" placeholder="Enter your full name" required>
                        </div>

                        <div class="form-row" style="margin-bottom:12px;">
                            <div class="col">
                                <label style="font-size:0.72rem; color:var(--gold-muted); text-transform:uppercase; letter-spacing:1.6px;">Phone *</label>
                                <input id="billing-phone" class="input" type="tel" placeholder="01XXXXXXXXX" required>
                            </div>
                            <div class="col">
                                <label style="font-size:0.72rem; color:var(--gold-muted); text-transform:uppercase; letter-spacing:1.6px;">Email *</label>
                                <input id="billing-email" class="input" type="email" placeholder="your@email.com" required>
                            </div>
                        </div>

                        <div style="margin-bottom:12px;">
                            <label style="font-size:0.72rem; color:var(--gold-muted); text-transform:uppercase; letter-spacing:1.6px;">State / City *</label>
                            <select id="billing-city" class="input" required>
                                <option value="">Select city</option>
                                <option value="Dhaka">Dhaka</option>
                                <option value="Chittagong">Chittagong</option>
                                <option value="Khulna">Khulna</option>
                            </select>
                        </div>

                        <div style="margin-bottom:12px;">
                            <label style="font-size:0.72rem; color:var(--gold-muted); text-transform:uppercase; letter-spacing:1.6px;">Street Address *</label>
                            <input id="billing-address" class="input" type="text" placeholder="House number and street name" required>
                        </div>

                        <div style="margin-bottom:12px;">
                            <label style="font-size:0.72rem; color:var(--gold-muted); text-transform:uppercase; letter-spacing:1.6px;">Order Notes (optional)</label>
                            <textarea id="billing-notes" class="input" rows="4" placeholder="Special notes for delivery..."></textarea>
                        </div>

                        <div style="display:flex; gap:12px; align-items:center; margin-top:16px;">
                            <input id="cod-checkbox" type="checkbox" checked style="width:18px; height:18px;">
                            <label for="cod-checkbox" style="font-size:0.92rem;">Cash on Delivery — Pay with cash upon receiving your luxury fragrance.</label>
                        </div>

                        <div style="margin-top:18px;">
                            <button type="submit" class="btn-gold" style="width:100%;">Place Order</button>
                        </div>
                    </form>
                </div>

                <aside class="checkout-panel">
                    <h3 style="margin-bottom:14px;">Your Order</h3>
                    <div id="checkout-items" style="max-height:360px; overflow:auto; margin-bottom:10px;"></div>

                    <div style="border-top:1px solid var(--border-gold); padding-top:12px;">
                        <div class="summary-row"><span>Subtotal</span><span id="checkout-subtotal">0৳</span></div>
                        <div class="summary-row"><span>Shipping</span><span id="checkout-shipping">0৳</span></div>
                        <div class="summary-total"><span>Total</span><span id="checkout-total">0৳</span></div>
                    </div>
                </aside>
            </div>
        </div>
    </section>

    <div id="cart-overlay" onclick="toggleCart(false)" aria-hidden="true">
        <div class="cart-drawer" onclick="event.stopPropagation()">
            <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:28px;">
                <h2 class="playfair" style="font-size: 1.6rem;">Selection</h2>
                <button onclick="toggleCart(false)" style="background:none; border:none; color:#fff;" aria-label="Close cart"><i data-lucide="x"></i></button>
            </div>
            <div id="cart-items" style="max-height: 55vh; overflow-y: auto; margin-bottom: 18px;"></div>
            <div style="border-top: 1px solid var(--border-gold); padding-top: 18px;">
                <div style="display:flex; justify-content:space-between; margin-bottom: 18px;">
                    <span style="text-transform:uppercase; letter-spacing:2px; font-size:0.8rem;">Subtotal</span>
                    <span id="cart-total" style="font-size:1.2rem; color:var(--gold-primary);">0৳</span>
                </div>
                <button onclick="checkout()" class="btn-gold" style="width:100%; padding:14px;">Complete Selection</button>
            </div>
        </div>
    </div>

    <footer>
        <img src="https://i.ibb.co.com/Vcq9NZsL/Logo.png" style="width: 50px; margin-bottom: 1.2rem; opacity: 0.5;">
        <p style="font-size: 0.7rem; letter-spacing: 4px; color: var(--gold-muted); text-transform: uppercase;">&copy; 2026 Luban Fragrance. Dhaka, Bangladesh</p>
    </footer>

    <script>
        lucide.createIcons();

        // Fixed BDT prices applied globally
        const PRICE_3ML_BDT = 200;
        const PRICE_6ML_BDT = 300;

        // Updated products list including all newly requested scents
        const products = [
            { "id": 1, "name": "Ehsas Al Arabia", "price3ml": 175.00, "price6ml": 310.00, "desc": "Oud, Rose, and Amber", "vibe": "Exotic, luxurious, and deeply Middle Eastern.", "img": "https://i.ibb.co.com/qYg6CR3M/ehsas-al-arabia.png" },
            { id: 2, name: "Vampire Blood", price3ml: 155.00, price6ml: 275.00, desc: "Red Berries, Jasmine, and Plum", vibe: "Dark, sweet, and gothic.", img: "https://i.ibb.co/VpWmC5gv/Red-and-Black-Modern-Black-Friday-Luxury-Fragrance-Perfume-Sale-Instagram-Post.png" },
            { id: 3, name: "CK One Inspired", price3ml: 95.00, price6ml: 165.00, desc: "Green Tea, Papaya, and Bergamot", vibe: "Clean, citrusy, and gender-neutral.", img: "https://images.unsplash.com/photo-1523293182086-7651a899d37f?auto=format&fit=crop&q=80&w=800" },
            { id: 4, name: "Scandal", price3ml: 145.00, price6ml: 255.00, desc: "Honey, Gardenia, and Blood Orange", vibe: "Bold, honey-sweet, and provocative.", img: "https://images.unsplash.com/photo-1590736704728-f4730bb30770?auto=format&fit=crop&q=80&w=800" },
            { id: 5, name: "Vanilla Dream", price3ml: 85.00, price6ml: 145.00, desc: "Warm Bean, Sugar, and Cream", vibe: "Comforting, sweet, and layering base.", img: "https://images.unsplash.com/photo-1616949755610-8c9bbc08f138?auto=format&fit=crop&q=80&w=800" },
            { id: 6, name: "Polo Blue Inspired", price3ml: 115.00, price6ml: 200.00, desc: "Cucumber, Melon, and Suede", vibe: "Fresh, aquatic ocean breeze.", img: "https://images.unsplash.com/photo-1551446339-1e5c6f164ec2?auto=format&fit=crop&q=80&w=800" },
            { id: 7, name: "Baccarat Rouge 540", price3ml: 210.00, price6ml: 375.00, desc: "Saffron, Jasmine, and Amberwood", vibe: "Airy, burnt-sugar sweetness.", img: "https://i.ibb.co/5zdJRRc/Baccarat-Rouge-540.png" },
            { id: 8, name: "Dior Sauvage Elixir", price3ml: 195.00, price6ml: 345.00, desc: "Cinnamon, Cardamom, and Licorice", vibe: "Spicy, masculine powerhouse.", img: "https://i.ibb.co/nhvh8bN/Dior-Sauvage-Elixir-copy-1.png" },
            { id: 9, name: "JPG Le Male", price3ml: 135.00, price6ml: 240.00, desc: "Mint, Lavender, and Vanilla", vibe: "Legendary soft-spicy balance.", img: "https://images.unsplash.com/photo-1603539322541-ad8278257db7?auto=format&fit=crop&q=80&w=800" },
            { id: 10, name: "Afnan 9pm", price3ml: 120.00, price6ml: 210.00, desc: "Apple, Cinnamon, and Vanilla", vibe: "Sweet, loud, party fragrance.", img: "https://images.unsplash.com/photo-1615396899839-c99c121888b0?auto=format&fit=crop&q=80&w=800" },
            { id: 11, name: "Creed Aventus", price3ml: 240.00, price6ml: 425.00, desc: "Pineapple, Birch, and Musk", vibe: "Smoky, fruity, sophisticated.", img: "https://i.ibb.co/dwwbGFFL/Creed-Aventus.png" },
            { "id": 12, "name": "Cool Water Man", "price3ml": 105.00, "price6ml": 185.00, "desc": "Sea Water, Mint, and Lavender", "vibe": "Fresh, aquatic blue classic.", "img": "https://i.ibb.co.com/6RrtfJzJ/Blue-and-White-Modern-Premium-Fragrance-Instagram-Story.jpg" },
            // NEW PRODUCTS START
            { id: 13, name: "YSL Y (EDP)", price3ml: 185.00, price6ml: 330.00, desc: "Green Apple, Ginger, and Sage", vibe: "Sharp, blue, and highly versatile with great longevity.", img: "https://i.ibb.co.com/xqtqgS4N/YSL-Y.png" },
            { "id": 14, "name": "Versace Eros", "price3ml": 140.00, "price6ml": 250.00, "desc": "Mint, Candy Apple, and Vanilla", "vibe": "Loud, sweet, and perfect for a vibrant night out.", "img": "https://i.ibb.co.com/nNz257xc/empty.png" },
            { id: 15, name: "Tobacco Vanille", price3ml: 225.00, price6ml: 400.00, desc: "Tobacco Leaf, Spices, and Creamy Vanilla", vibe: "Rich, opulent, and smells like a private luxury lounge.", img: "https://i.ibb.co.com/S4QFM16c/Tobacco-Vanilla.png" },
            { id: 16, name: "Hawas Ice", price3ml: 130.00, price6ml: 230.00, desc: "Frozen Apple, Bergamot, and Mint", vibe: "Louder, fresher, and colder than the original DNA.", img: "https://i.ibb.co.com/DgSB4fJ8/hawas-Ice.png" },
            { id: 17, name: "Hawas Fire", price3ml: 135.00, price6ml: 240.00, desc: "Spices, Leather, and Intense Woods", vibe: "A warm, intense version ideal for cooler weather.", img: "https://i.ibb.co.com/HDYqMHsd/Gemini-Generated-Image-7f1xcp7f1xcp7f1x.png" },
            { id: 18, name: "Stronger With You", price3ml: 160.00, price6ml: 285.00, desc: "Chestnut, Cardamom, and Vanilla", vibe: "Warm, sweet, and cozy—the ultimate date night scent.", img: "https://i.ibb.co.com/SDmwnHSH/Stronger-with-You.png" },
            { id: 19, name: "Acqua Di Gio", price3ml: 110.00, price6ml: 195.00, desc: "Sea Notes, Lime, and Jasmine", vibe: "The gold standard for aquatics—clean, light, and airy.", img: "https://i.ibb.co.com/nNz257xc/empty.png" },
            { id: 20, name: "Paris Hilton (Men)", price3ml: 90.00, price6ml: 155.00, desc: "Cucumber and Mango Profile", vibe: "A fruity, fresh profile that is casual and easy-going.", img: "https://images.unsplash.com/photo-1541605027-0b81e3f22330?auto=format&fit=crop&q=80&w=800" },
            { id: 21, name: "Sultan (Al-Rehab)", price3ml: 80.00, price6ml: 140.00, desc: "Cedar, Juniper, and Musk", vibe: "Concentrated woodiness—very masculine and robust.", img: "https://i.ibb.co.com/1fFTxNF9/sultan.png" }
        ];

        let cart = [];
        let selectedSize = '3ml';

        // UI Interactions
        function toggleMobileMenu() {
            const menu = document.getElementById('mobile-menu');
            const toggle = document.querySelector('.menu-toggle');
            const expanded = (toggle && toggle.getAttribute('aria-expanded') === 'true');
            menu.classList.toggle('active');
            const isActive = menu.classList.contains('active');
            if (toggle) toggle.setAttribute('aria-expanded', isActive ? 'true' : 'false');
            menu.setAttribute('aria-hidden', isActive ? 'false' : 'true');
        }

        function handleNavClick(id) {
            showView('main');
            setTimeout(() => {
                const el = document.getElementById(id);
                if(el) el.scrollIntoView({behavior:'smooth'});
            }, 120);
        }

        // Initialize Shop Grid
        function renderShop() {
            const list = document.getElementById('product-list');
            list.innerHTML = ''; // Clear previous
            products.forEach(p => {
                const card = document.createElement('div');
                card.className = 'product-card';
                card.onclick = () => showView('details', p.id);
                card.innerHTML = `
                    <div class="image-container">
                        <img src="${p.img}" alt="${p.name}" loading="lazy" onerror="this.src='https://images.unsplash.com/photo-1557170334-a9632e77c6e4?auto=format&fit=crop&q=80&w=800'">
                    </div>
                    <div style="display:flex; justify-content:space-between; align-items:baseline; margin-bottom:8px;">
                        <h3 style="font-size:1.05rem;">${p.name}</h3>
                        <span style="color:var(--gold-primary); font-weight:500; font-size:0.9rem;">3 ML – ${PRICE_3ML_BDT}৳ &nbsp;•&nbsp; 6 ML – ${PRICE_6ML_BDT}৳</span>
                    </div>
                    <p style="font-size:0.65rem; color:var(--gold-muted); text-transform:uppercase; letter-spacing:1.2px;">${p.desc}</p>
                `;
                list.appendChild(card);
            });
            lucide.createIcons();
        }

        function showView(view, id) {
            const main = document.getElementById('main-view');
            const detailView = document.getElementById('details-view');
            const checkoutView = document.getElementById('checkout-view');
            if(view === 'details') {
                const p = products.find(x => x.id === id);
                selectedSize = '3ml'; 
                renderDetails(p);
                main.style.display = 'none';
                checkoutView.style.display = 'none';
                detailView.style.display = 'block';
            } else if (view === 'checkout') {
                main.style.display = 'none';
                detailView.style.display = 'none';
                checkoutView.style.display = 'block';
                renderCheckout();
            } else {
                main.style.display = 'block';
                detailView.style.display = 'none';
                checkoutView.style.display = 'none';
            }
            window.scrollTo({top: 0, behavior: 'smooth'});
        }

        function setSize(size, productId, el) {
            selectedSize = size;
            // toggle active class for size buttons
            document.querySelectorAll('.size-btn').forEach(btn => btn.classList.remove('active'));
            if (el && el.classList) el.classList.add('active');

            const priceEl = document.getElementById('detail-price');
            if (priceEl) priceEl.innerText = `${size === '3ml' ? PRICE_3ML_BDT : PRICE_6ML_BDT}৳`;
        }

        function renderDetails(p) {
            document.getElementById('details-content').innerHTML = `
                <div class="details-grid">
                    <div style="background:var(--bg-accent); border:1px solid var(--border-gold); aspect-ratio:1/1; display:flex; align-items:center; justify-content:center; padding:30px; border-radius:12px;">
                        <img src="${p.img}" style="max-width:100%; max-height:100%; object-fit:contain;" onerror="this.src='https://images.unsplash.com/photo-1557170334-a9632e77c6e4?auto=format&fit=crop&q=80&w=800'">
                    </div>
                    <div>
                        <span onclick="showView('main')" style="cursor:pointer; color:var(--gold-primary); font-size:0.72rem; text-transform:uppercase; letter-spacing:3px; display:inline-flex; align-items:center; gap:8px; margin-bottom:18px;">
                            <i data-lucide="arrow-left" size="14"></i> Return to Collection
                        </span>
                        <h2 style="font-size:clamp(1.8rem, 4.6vw, 3rem); margin-bottom:8px;">${p.name}</h2>
                        <div id="detail-price" style="font-size:1.4rem; color:var(--gold-primary); margin-bottom:14px;">${PRICE_3ML_BDT}৳</div>
                        
                        <p style="text-transform:uppercase; font-size:0.72rem; color:var(--gold-muted); letter-spacing:2px; margin-bottom:10px;">Select Volume</p>
                        <div class="size-selector">
                            <button class="size-btn active" onclick="setSize('3ml', ${p.id}, this)">3 mL</button>
                            <button class="size-btn" onclick="setSize('6ml', ${p.id}, this)">6 mL</button>
                        </div>

                        <p style="font-style:italic; color:var(--text-dim); margin-bottom:20px; border-left:2px solid var(--gold-primary); padding-left:16px;">"${p.vibe}"</p>
                        <p style="color:var(--text-dim); margin-bottom:22px;">Detailed formulation with notes of ${p.desc}. Hand-selected essences from the finest sources around the globe.</p>
                        <button class="btn-gold" style="width:100%" onclick="addToCart(${p.id})">Add to Selection</button>
                    </div>
                </div>
            `;
            lucide.createIcons();
        }

        // Use class toggles for cart overlay to simplify responsive CSS handling
        function toggleCart(open) {
            const overlay = document.getElementById('cart-overlay');
            if(open) {
                overlay.classList.add('open');
                overlay.setAttribute('aria-hidden', 'false');
                renderCart();
            } else {
                overlay.classList.remove('open');
                overlay.setAttribute('aria-hidden', 'true');
            }
        }

        function addToCart(id) {
            const p = products.find(x => x.id === id);
            const currentPrice = selectedSize === '3ml' ? PRICE_3ML_BDT : PRICE_6ML_BDT;
            const cartId = `${id}-${selectedSize}`;
            const exists = cart.find(x => x.cartId === cartId);
            
            if(exists) {
                exists.qty++;
            } else {
                cart.push({
                    ...p, 
                    cartId: cartId, 
                    selectedSize: selectedSize, 
                    activePrice: currentPrice, 
                    qty: 1
                });
            }
            
            updateCartCount();
            toggleCart(true);
        }

        function renderCart() {
            const cont = document.getElementById('cart-items');
            cont.innerHTML = cart.length ? '' : '<p style="text-align:center; opacity:0.32; margin-top:40px;">Your selection is empty</p>';
            let subtotal = 0;
            cart.forEach(item => {
                subtotal += item.activePrice * item.qty;
                const div = document.createElement('div');
                div.style = 'display:flex; align-items:center; gap:14px; margin-bottom:14px; padding-bottom:14px; border-bottom:1px solid var(--border-gold)';
                div.innerHTML = `
                    <div style="width:50px; height:50px; background:#000; display:flex; align-items:center; justify-content:center; flex-shrink:0; border-radius:8px; overflow:hidden">
                        <img src="${item.img}" style="max-width:80%; max-height:80%; object-fit:contain" onerror="this.src='https://images.unsplash.com/photo-1557170334-a9632e77c6e4?auto=format&fit=crop&q=80&w=800'">
                    </div>
                    <div style="flex:1">
                        <h4 class="playfair" style="font-size:0.98rem; margin-bottom:4px;">${item.name} (${item.selectedSize})</h4>
                        <p style="font-size:0.78rem; color:var(--gold-muted)">${item.activePrice}৳ x ${item.qty}</p>
                    </div>
                    <i data-lucide="x" size="16" style="cursor:pointer; opacity:0.6" onclick="removeFromCart('${item.cartId}')"></i>
                `;
                cont.appendChild(div);
            });
            document.getElementById('cart-total').innerText = `${subtotal}৳`;
            lucide.createIcons();
        }

        function removeFromCart(cartId) {
            cart = cart.filter(x => x.cartId !== cartId);
            renderCart();
            updateCartCount();
        }

        function updateCartCount() {
            document.getElementById('cart-count').innerText = cart.reduce((a,b) => a+b.qty, 0);
        }

        // NEW: render checkout page populated from cart
        function renderCheckout() {
            const itemsEl = document.getElementById('checkout-items');
            const subtotalEl = document.getElementById('checkout-subtotal');
            const shippingEl = document.getElementById('checkout-shipping');
            const totalEl = document.getElementById('checkout-total');

            itemsEl.innerHTML = '';
            let subtotal = 0;
            if (!cart.length) {
                itemsEl.innerHTML = '<p style="opacity:0.32;">Your selection is empty. Add some fragrances first.</p>';
            } else {
                cart.forEach(item => {
                    subtotal += item.activePrice * item.qty;
                    const row = document.createElement('div');
                    row.style = 'display:flex; gap:12px; align-items:center; margin-bottom:12px;';

                    row.innerHTML = `
                        <div style="width:54px; height:54px; border-radius:8px; overflow:hidden; background:#000; display:flex; align-items:center; justify-content:center;">
                            <img src="${item.img}" style="max-width:88%; max-height:88%; object-fit:contain" onerror="this.src='https://images.unsplash.com/photo-1557170334-a9632e77c6e4?auto=format&fit=crop&q=80&w=800'">
                        </div>
                        <div style="flex:1">
                            <div style="font-weight:600;">${item.name} <span style="font-weight:400; color:var(--text-dim); font-size:0.9rem;">(${item.selectedSize})</span></div>
                            <div style="color:var(--text-dim); font-size:0.9rem;">${item.qty} x ${item.activePrice}৳</div>
                        </div>
                        <div style="font-weight:700; color:var(--gold-primary);">${item.activePrice * item.qty}৳</div>
                    `;
                    itemsEl.appendChild(row);
                });
            }

            // simple shipping calc: free over 2000, else flat 70
            const shipping = subtotal === 0 ? 0 : (subtotal >= 2000 ? 0 : 70);
            const total = subtotal + shipping;

            subtotalEl.innerText = `${subtotal}৳`;
            shippingEl.innerText = `${shipping}৳`;
            totalEl.innerText = `${total}৳`;
        }

        // Updated checkout: show checkout view (instead of external redirect)
        function checkout() {
            showView('checkout');
        }

        function placeOrder() {
            const name = document.getElementById('billing-name').value.trim();
            const phone = document.getElementById('billing-phone').value.trim();
            const email = document.getElementById('billing-email').value.trim();
            const city = document.getElementById('billing-city').value;
            const address = document.getElementById('billing-address').value.trim();

            if (!name || !phone || !email || !city || !address) {
                alert('Please complete all required billing fields before placing your order.');
                return;
            }

            if (!cart.length) {
                alert('Your selection is empty. Please add items to your cart before placing an order.');
                return;
            }

            // Build order object
            const order = {
                id: 'LB' + Date.now(),
                name, phone, email, city, address,
                notes: document.getElementById('billing-notes').value.trim(),
                items: cart.map(i => ({ id: i.id, name: i.name, size: i.selectedSize, qty: i.qty, price: i.activePrice })),
                subtotal: Number(document.getElementById('checkout-subtotal').innerText.replace('৳','')) || 0,
                shipping: Number(document.getElementById('checkout-shipping').innerText.replace('৳','')) || 0,
                total: Number(document.getElementById('checkout-total').innerText.replace('৳','')) || 0,
                payment: document.getElementById('cod-checkbox').checked ? 'Cash on Delivery' : 'Other'
            };

            // Send order to Google Apps Script Web App
            // NOTE: per your instruction, this exact URL must be used (no leading/trailing slashes changed).
            fetch("https://script.google.com/macros/s/AKfycbwsFOVCDS8AjG4tBTqKgdnIMMeM-zfc1Zj0xgWOhnBG-aLKohM0K_3XRGyCOmPhoCviLw/exec", {
                method: "POST",
                headers: {
                    "Content-Type": "application/json"
                },
                body: JSON.stringify(order)
            })
            .then(response => {
                // try to parse JSON; some implementations may return plain text
                return response.json().catch(() => ({ success: false, status: response.status }));
            })
            .then(result => {
                console.log("Apps Script response:", result);
            })
            .catch(err => {
                console.error("Failed sending to Apps Script:", err);
            })
            .finally(() => {
                // For this static demo: show confirmation overlay and clear cart (same UX as before)
                const msg = `Thank you ${order.name}! Your order (${order.id}) has been placed.\nTotal: ${order.total}৳\nPayment: ${order.payment}`;
                alert(msg);

                // clear cart
                cart = [];
                updateCartCount();
                renderCart();

                // show main view
                showView('main');
            });
        }

        // Global Effects
        window.addEventListener('scroll', () => {
            const scrollY = window.scrollY;
            const totalHeight = document.documentElement.scrollHeight - window.innerHeight;
            
            const progress = document.getElementById('scroll-progress');
            if (totalHeight > 0) progress.style.width = (scrollY / totalHeight * 100) + '%';
            document.getElementById('navbar').classList.toggle('scrolled', scrollY > 60);
            document.getElementById('back-to-top').classList.toggle('visible', scrollY > 500);
            
            const bgText = document.querySelector('.hero-bg-text');
            if(bgText && document.getElementById('main-view').style.display !== 'none') {
                bgText.style.transform = `translateX(${scrollY * 0.18}px)`;
            }
        });

        document.addEventListener('mousemove', (e) => {
            const cursor = document.getElementById('custom-cursor');
            if(cursor && window.innerWidth >= 1200) {
                cursor.style.left = e.clientX + 'px';
                cursor.style.top = e.clientY + 'px';
            }
        });

        // Accessibility: close mobile menu with escape
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                const mobile = document.getElementById('mobile-menu');
                if (mobile.classList.contains('active')) toggleMobileMenu();
                const overlay = document.getElementById('cart-overlay');
                if (overlay.classList.contains('open')) toggleCart(false);
            }
        });

        // Load content
        window.onload = renderShop;

        // Ensure layout responds when resizing (useful for drawer behavior)
        window.addEventListener('resize', () => {
            // close overlays when switching between mobile/desktop to avoid stuck drawer
            const overlay = document.getElementById('cart-overlay');
            if (!overlay.classList.contains('open')) return;
            // keep it open, but ensure CSS handles position — no extra logic required here
        });
    </script>
</body>
</html>
